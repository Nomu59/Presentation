<form method="post" action="{{ path('app_admin_chambre_delete', {'id': chambre.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chambre.id) }}">
    <button class="btn">Supprimer</button>
</form>
